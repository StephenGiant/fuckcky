<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="hospitalpremodel"
            type="com.nsk.app.bussiness.healthy.viewmodel.HealthyCategoriBean.NskHospitalInfoBean"/>
    </data>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingLeft="@dimen/space_13"
    android:paddingTop="@dimen/space_12"
    android:paddingBottom="@dimen/space_12"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
<ImageView
    android:scaleType="fitCenter"
    android:id="@+id/iv_hospitallogo"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    android:layout_width="0dp"
    android:layout_height="@dimen/space_30"
    app:layout_constraintDimensionRatio="h,1:4.4"
    app:url="@{hospitalpremodel.n_hospital_logo}"
    />
    <TextView
        android:layout_marginTop="@dimen/space_2"
        android:id="@+id/tv_hostitle"
        app:layout_constraintTop_toBottomOf="@id/iv_hospitallogo"
        android:textColor="@color/title_black"
        android:textSize="15sp"
        android:text="@{hospitalpremodel.n_hospital_name}"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <TextView
        android:text="@{hospitalpremodel.n_treatment}"
        android:layout_marginTop="@dimen/space_2"
        app:layout_constraintTop_toBottomOf="@id/tv_hostitle"
        android:id="@+id/tv_hossubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textColor="@color/boder_gray"
        />
</android.support.constraint.ConstraintLayout>
</layout>